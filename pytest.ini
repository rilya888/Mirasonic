[pytest]
markers =
    live: hits real network (YouTube Music) — run explicitly with `-m live`
addopts = -m "not live"
# Pinned rather than left to the default: pytest-asyncio has changed its
# default mode before, and every async test here carries the marker already.
asyncio_mode = strict
